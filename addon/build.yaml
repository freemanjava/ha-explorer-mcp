build_from:
  aarch64: "homeassistant/aarch64-base:3.18"
  amd64: "homeassistant/amd64-base:3.18"

# CGO_ENABLED=0 static binary — no arch-specific args needed.
args: {}

labels:
  org.opencontainers.image.source: "https://github.com/freemanjava/ha-explorer-mcp"
